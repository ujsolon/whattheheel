---
scope: What the Heel: full-stack Next.js app - anonymous Trendsetter Feed, client-side manual shoe overlay preview, registration/auth with selfie capture, YouCam AI Shoes VTO integration, retail links
purpose: build-substrate
altitude: feature
updated: 2026-08-17T06:36
---

- (version) [ADOPTED] Next.js v16.0.3, TypeScript v5, React v19.2.0, Tailwind CSS v4, ESLint v9 + eslint-config-next (from project-context.md)
- (constraint) [ADOPTED] FR-02: anonymous preview is manual drag/scale/rotate overlay, client-side only (Canvas/CSS transforms + pointer events) - no ML model, no API call, zero marginal cost per anon session (PRD)
- (constraint) [ADOPTED] FR-04/FR-06: registered-tier VTO via YouCam /s2s/v2.0/task/shoes - async task+poll, error codes (error_no_face, error_download_image, error_inference, error_nsfw_content_detected, exceed_max_filesize, invalid_parameter) mapped to inline per-code UI copy (PRD)
- (constraint) [ADOPTED] Image constraints: selfie >=512x512 face>15%height half-body-to-waist framing; shoe product img >=512x512 shoe>25%height; shoe worn img >=800x800 shoe>20%height single-item; all jpg/jpeg/png/heic <10MB (PRD NFR, sourced docs/perfectcorp-api-reference.md)
- (constraint) [ADOPTED] Cost isolation: each YouCam VTO call = 2 units; VTO calls restricted to registered users only, anon tier stays fully client-side to protect free-tier budget (PRD)
- (constraint) [ADOPTED] Security: API keys/secrets via env vars only, never hardcoded (project-context.md, PRD NFR)
- (constraint) [ADOPTED] Testing: Jest + React Testing Library, co-located __tests__ dirs, *.test.tsx naming, every new component gets a render-smoke test (project-context.md)
- (constraint) [ADOPTED] Component conventions: reusable components in app/components/ PascalCase, Next.js App Router file-based routing, prefer Server Components, 'use client' only for interactive UI, useState for local state / avoid global state lib unless necessary (project-context.md)
- (version) Node.js 24 (Active LTS through 2028-04, verified web) as pinned runtime for Next.js 16 (requires >=20.9) - user preference, confirmed compatible
- (decision) DB: MongoDB Atlas free M0 tier (512MB, permanent free, verified web) for structured/semi-structured data - user profiles, VTO task records, trend feed cache. Flexible schema fits YouCam's varying task response shapes
- (decision) Image storage: Cloudinary (not MongoDB/GridFS) for raw selfie + shoe image bytes - user chose over Vercel Blob/S3. MongoDB holds only Cloudinary URL + metadata. Rationale: selfies up to 10MB approach BSON 16MB doc cap; industry-standard pattern is object storage for bytes + DB for metadata (verified web)
- (version) cloudinary npm SDK v2.10.0 (verified web, npmjs.com/package/cloudinary), optionally next-cloudinary for component/helper layer
- (decision) Paradigm: layered architecture on Next.js App Router. app/api/*/route.ts (thin HTTP layer) -> lib/services/* (business logic, no Next.js imports) -> lib/data/* (Mongo repositories) + lib/external/* (YouCam, Cloudinary clients). One-way dependency: routes->services->data/external, never reversed
- (constraint) All YouCam/Cloudinary/MongoDB calls server-side only, Node.js runtime (not Edge - Mongo driver + Cloudinary SDK need full Node APIs). Prevents API key exposure to client, satisfies PRD NFR-Security
- (decision) VTO task lifecycle owned by single service lib/services/vtoTask.ts writing to single vto_tasks Mongo collection (task_id, status, error_code, src/ref cloudinary urls, timestamps). Client polls single /api/vto-tasks/[id] status route, never calls YouCam directly. Prevents divergent polling/error-mapping logic across independently-built clients
- (decision) Image validation (size/format/dimensions per PRD Image Constraints) happens once server-side in upload route before forwarding to Cloudinary - not duplicated client+server
- (decision) C:/Program Files/Git/api/vto-tasks (create) requires authenticated session - enforces PRD cost-isolation (registered-only VTO) at server boundary not just UI
- (decision) Auth: Auth.js v5 + @auth/mongodb-adapter (verified web: stable, works with Next.js 16 App Router and MongoDB). Session-based. Anonymous Journey 1 has zero auth touchpoints
- (assumption) [ASSUMPTION] Deployment target: Vercel (natural fit for Next.js, not yet explicitly confirmed by user). .env.local for dev, Vercel project env vars for preview/prod. Single MongoDB Atlas M0 cluster, separate whattheheel_dev/whattheheel_prod database names
- (decision) MongoDB collections (seed): users (Auth.js-owned + profile refs), vto_tasks (task lifecycle), trends (curated JSON-backed feed per FR-01, seeded/read-only at runtime, not user-writable)
- (decision) CORRECTION of entry 24 (shell mangled the leading slash): api-slash-vto-tasks POST route (create) requires authenticated session - enforces PRD cost-isolation (registered-only VTO) at server boundary not just UI
- (version) Auth: next-auth v4.24.15 (stable, verified npm registry directly - v5/5.0.0-beta.32 is still beta despite many 2026 tutorials assuming it) + @next-auth/mongodb-adapter (v4-compatible). User chose stability over App Router native ergonomics
- (version) mongodb npm driver v7.5.0 (verified npm registry)
- (event) Reviewer gate run: lint clean, 5 parallel subagent reviews (PRD reconcile, project-context reconcile, rubric walker, version verification, adversarial divergence). Fixes applied: AD-1 forbids NEXT_PUBLIC_ secret prefix; AD-3 strengthened to service-level auth+ownership check (fixes service-bypass and IDOR holes); AD-6 excludes invalid_parameter from user-facing inline errors; trends corrected from Mongo collection to static public/trends.json seed; users/user_profiles split to resolve NextAuth-adapter dual-writer conflict; userId added to vto_tasks and typed as string; buyUrl added to TREND; success envelope convention added; @next-auth/mongodb-adapter pinned to 1.1.3 with staleness callout; 6 Deferred items added (curated-image conformance, polling UX, dev-vs-billing, bundling gotcha, version drift, CI/CD)
- (event) spine finalized
- (decision) Deployment target confirmed as Vercel (user explicit confirmation, promoted from prior [ASSUMPTION]). Node.js 24 set via package.json engines.node - this is what Vercel Functions read to select runtime, not a separately provisioned server. Vercel deprecates Node 20 as Functions option 2026-10-01 (verified web) - explicit pin avoids drifting onto a soon-cut-off default
- (event) UPDATE RUN: auth method decided during UX Discovery as NextAuth CredentialsProvider (email+password), not OAuth. This invalidates the prior assumption that @next-auth/mongodb-adapter (OAuth-shaped) fully owns the users collection - reopening that portion of the spine
- (constraint) Verified web (NextAuth v4 docs/FAQ, GitHub discussions): CredentialsProvider requires session: {strategy: 'jwt'} - incompatible with database-persisted sessions. This resolves the prior Deferred item 'NextAuth session strategy (JWT vs DB sessions)' - no longer open, forced to JWT by the Credentials choice
- (decision) Drop @next-auth/mongodb-adapter (1.1.3) from Stack entirely - it exists to persist OAuth accounts/sessions via the adapter interface, neither of which applies to Credentials+JWT. No adapter is used. This also removes the '--legacy-peer-deps stale adapter' risk noted at original finalization
- (decision) users collection ownership reversed: now fully app-owned via lib/data/users.ts (id, email, passwordHash, createdAt), written only by a new registration route, read only inside CredentialsProvider's authorize() callback. user_profiles stays a separate collection (Story 2.2 already built on this split) - no longer forced by adapter ownership, kept as a deliberate organizational choice (auth-core fields vs app-profile fields)
- (version) bcryptjs 3.0.3 (verified web) chosen over native bcrypt 6.0.0 for password hashing - pure JS, no node-gyp build step, works reliably on Vercel serverless Functions (native bcrypt has known friction in serverless/edge deploy environments)
- (event) spine updated: AD-7 added (Credentials-based auth ownership + JWT session strategy), @next-auth/mongodb-adapter dropped, bcryptjs added, users collection ownership reversed to app-owned, Deferred session-strategy item resolved. Light reviewer check (no parallel subagents) - internally consistent, lint clean
- (event) Removed resolved Deferred item 'Polling UX during VTO inference' - now concretely decided in the UX EXPERIENCE.md spine (2s poll interval, progress bar, rotating status copy, 30s fallback). Linked EXPERIENCE.md/DESIGN.md as companions in frontmatter
